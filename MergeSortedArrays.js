'use strict';

// rules : must be sorted inside nums1, which will have a length of m+n

let nums1 = [1, 2, 3, 4, 0, 0, 0];
let nums2 = [2, 4, 6];

let m = 4;
let n = 3;

function merge(n1, m, n2, n) {

  // add items to a single array (On)
  for (let i = 0, j = 0; i < n1.length; i++) {
      // check if n2 i is le
  }

  //

}
